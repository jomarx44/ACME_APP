import { View, Text } from "react-native";

const Maintenance = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text> Under on development.. </Text>
        </View>
    );
}

export default Maintenance